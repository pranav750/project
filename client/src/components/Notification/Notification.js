import Loading from "./Loading";
import Message from "./Message";

const Notification = () => {
  return (
    <div>
      <Loading />
      <Message />
    </div>
  );
};

export default Notification;
